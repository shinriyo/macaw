{
  "name": "macaw",
  "displayName": "Macaw",
  "description": "Automatically changes workspace color and title based on folder path or active language.",
  "version": "0.0.1",
  "publisher": "shinriyo",
  "engines": {
    "vscode": "^1.90.0"
  },
  "categories": [
    "Other"
  ],
  "files": [
    "dist",
    "README.md"
  ],
  "activationEvents": [
    "onStartupFinished",
    "onCommand:macaw.apply",
    "onCommand:macaw.clear",
    "onCommand:macaw.openRules"
  ],
  "main": "./dist/extension.js",
  "contributes": {
    "commands": [
      {
        "command": "macaw.apply",
        "title": "Macaw: Apply"
      },
      {
        "command": "macaw.clear",
        "title": "Macaw: Clear"
      },
      {
        "command": "macaw.openRules",
        "title": "Macaw: Open Rules"
      }
    ],
    "configuration": {
      "title": "Macaw",
      "properties": {
        "macaw.colorMode": {
          "type": "string",
          "enum": [
            "path",
            "language",
            "none"
          ],
          "default": "path",
          "description": "Controls how Macaw chooses the workspace color."
        },
        "macaw.pathRules": {
          "type": "array",
          "default": [
            {
              "pattern": "~/develop/*",
              "label": "DEV",
              "color": "#7E57C2"
            },
            {
              "pattern": "~/work/*",
              "label": "WORK",
              "color": "#1976D2"
            },
            {
              "pattern": "~/danger/*",
              "label": "DANGER",
              "color": "#D32F2F"
            }
          ],
          "items": {
            "type": "object",
            "required": [
              "pattern",
              "label",
              "color"
            ],
            "properties": {
              "pattern": {
                "type": "string"
              },
              "label": {
                "type": "string"
              },
              "color": {
                "type": "string",
                "pattern": "^#[0-9A-Fa-f]{6}$"
              }
            }
          },
          "description": "Root path to label/color mappings. Use the Macaw: Open Rules command to edit them without touching JSON. Rules are checked in order; the first matching rule wins."
        },
        "macaw.languageColors": {
          "type": "object",
          "default": {
            "typescript": "#3178C6",
            "javascript": "#F7DF1E",
            "python": "#3776AB",
            "rust": "#CE422B",
            "go": "#00ADD8",
            "dart": "#0175C2"
          },
          "additionalProperties": {
            "type": "string",
            "pattern": "^#[0-9A-Fa-f]{6}$"
          },
          "description": "Language ID to color mappings used when Color Mode is Language."
        },
        "macaw.showLanguageInTitle": {
          "type": "boolean",
          "default": true,
          "description": "Show the active language in the window title."
        },
        "macaw.showPathLabelInTitle": {
          "type": "boolean",
          "default": true,
          "description": "Show the matched path label in the window title."
        }
      }
    }
  },
  "scripts": {
    "compile": "tsc -p ./",
    "watch": "tsc -watch -p ./",
    "vscode:prepublish": "npm run compile"
  },
  "devDependencies": {
    "@types/node": "^20.14.0",
    "@types/vscode": "^1.90.0",
    "typescript": "^5.4.5"
  }
}
